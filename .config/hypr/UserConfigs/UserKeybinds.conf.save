# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# This is where you put your own keybinds. Be Mindful to check as well ~/.config/hypr/configs/Keybinds.conf to avoid conflict
# if you think I should replace the Pre-defined Keybinds in ~/.config/hypr/configs/Keybinds.conf , submit an issue or let me know in DC and present me a valid reason as to why, such as conflicting with global shortcuts, etc etc

# See https://wiki.hyprland.org/Configuring/Keywords/ for more settings and variables
# See also Laptops.conf for laptops keybinds 

$mainMod = SUPER
$files = dolphin
$term = kitty
$scriptsDir = $HOME/.config/hypr/scripts
$UserScripts = $HOME/.config/hypr/UserScripts

# rofi App launcher
#bindr = $mainMod, $mainMod_L, exec, pkill rofi || rofi -show drun -modi  drun,filebrowser,run,window # Super Key to Launch rofi menu
#bind = $mainMod, D, exec, pkill rofi || /home/anti/.config/rofi/scripts/gtk-launcher.sh -show drun gtk-spotlight
#bindr = $mainMod, exec, $HOME/.config/rofi/launchers/type-6/launcher.sh
bindr = $mainMod, $mainMod_L, exec, pkill rofi || $HOME/.config/rofi/launchers/type-6/launcher.sh


#kodi
bind = $mainMod, K, exec, kodi

#pypr
bind = $mainMod, S, togglespecialworkspace, stash
bind = $mainMod ALT, s, exec, pypr toggle_special stash
#bind = $mainMod Alt, Return, exec, pypr toggle term
#bind = $mainMod, r, exec, pypr toggle ranger
#bind = $mainMod ALT, r, exec, pypr toggle ranger

# ags overview
#bind = $mainMod, A, exec, pkill rofi || true && ags -t 'overview'



#bind = $mainMod, r, exec, pkill ghostty ||  ghostty --class=com.ghostty.ranger -e ranger
windowrulev2 = float, class:^(com.ghostty.ranger)$
windowrulev2 = size 60% 50%, class:^(com.ghostty.ranger)$
windowrulev2 = move 25% 49%, class:^("com.ghostty.ranger")$

#bind = $mainMod, Return, exec,pkill kitty ||  kitty --class dropdown -T dropdown 
windowrulev2 = float, title:^(kitty2)$
windowrulev2 = size 75% 60%, title:^(kitty2)$
windowrulev2 = move 10% 37%, title:^(kitty2)$

#bind = $mainMod, t, exec,pkill kitty ||  kitty --class kitty-dropdown -T kitty-dropdown

#bind = $mainMod, r, exec, terminology -e ranger
bind = $mainMod, T, exec, kitty -o include='~/.config/kitty/kitty-themes/Glacier.conf'  # Launch terminal
bind = $mainMod, F, exec, thunar # Launch file manager

bind = $mainMod ALT, C, exec, $UserScripts/RofiCalc.sh # calculator (qalculate)

# pyprland
bind = $mainMod , Return, exec, pypr toggle term # Dropdown terminal


bind = $mainMod, Z, exec, pypr zoom # Toggle Zoom

# User Added Keybinds
bind = $mainMod SHIFT, O, exec, $UserScripts/ZshChangeTheme.sh # Change oh-my-zsh theme
bindn = ALT_L, SHIFT_L, exec, $scriptsDir/SwitchKeyboardLayout.sh # Changing the keyboard layout

# For passthrough keyboard into a VM
# bind = $mainMod ALT, P, submap, passthru
#submap = passthru
# to unbind
#bind = $mainMod ALT, P, submap, reset
#submap = reset
